# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod1

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
#font pango:tewi 8
font pango:SauceCodePro Nerd Font Mono 11


# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVuSansM Nerd Font Mono 8

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
#bindsym $mod+n exec TERMINAL=rxvt-unicode i3-sensible-terminal
set $alt-font   \\"xft:tewi:size=11:antialias=false\\"
set $term-pwd   $(xcwd)

bindsym $mod+n exec urxvtc -cd $term-pwd
bindsym $mod+shift+n exec urxvtc -cd $term-pwd -fn $alt-font
#bindsym $mod+Return

# kill focused window
bindsym $mod+Shift+q kill


# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
#bindsym $mod+Left focus left
#bindsym $mod+Down focus down
#bindsym $mod+Up focus up
#bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
#bindsym $mod+Shift+Left move left
#bindsym $mod+Shift+Down move down
#bindsym $mod+Shift+Up move up
#bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+b split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
#bindsym $mod+s layout stacking
bindsym $mod+s layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+q focus parent

# focus the child container
#bindsym $mod+d focus child

# workspace switches
bindsym $mod+ampersand workspace	$wksp1
bindsym $mod+eacute workspace 		$wksp2
bindsym $mod+quotedbl workspace 	$wksp3
bindsym $mod+apostrophe workspace 	$wksp4
bindsym $mod+parenleft workspace 	$wksp5
bindsym $mod+minus workspace 		$wksp6
bindsym $mod+egrave workspace 		$wksp7
bindsym $mod+underscore workspace 	$wksp8
bindsym $mod+ccedilla workspace 	$wksp9
bindsym $mod+agrave workspace 		$wksp10

# workspace aliases

set $wksp-tasks 	$wksp1
set $wksp-home		$wksp2
set $wksp-term		$wksp3
set $wksp-vim		$wksp4
set $wksp-browse 	$wksp5
set $wksp-find 		$wksp6
set $wksp-a7    	$wksp7
set $wksp-a8    	$wksp8
set $wksp-a9    	$wksp9
set $wksp-a10    	$wksp10

# workspace icons

set $wksp1    "1 "
set $wksp2    "2 "
set $wksp3    "3 "
set $wksp4    "4 "
set $wksp5    "5 "
set $wksp6    "6 "
set $wksp7    "7  "
set $wksp8    "8  "
set $wksp9    "9  "
set $wksp10   "10 "


# bind to back and forth workspaces
workspace_auto_back_and_forth yes
bindsym $mod+BackSpace workspace back_and_forth

# move focused container to workspace
bindsym $mod+Shift+ampersand move container to	    workspace $wksp1
bindsym $mod+Shift+eacute move container to 		workspace $wksp2
bindsym $mod+Shift+quotedbl move container to 		workspace $wksp3
bindsym $mod+Shift+apostrophe move container to 	workspace $wksp4
bindsym $mod+Shift+parenleft move container to 	    workspace $wksp5
bindsym $mod+Shift+minus move container to   		workspace $wksp6
bindsym $mod+Shift+egrave move container to  		workspace $wksp7
bindsym $mod+Shift+underscore move container to 	workspace $wksp8
bindsym $mod+Shift+ccedilla move container to 		workspace $wksp9
bindsym $mod+Shift+agrave move container to 		workspace $wksp10
# Prior = PageUp
bindsym $mod+Prior workspace prev
# Next = PageDown
bindsym $mod+Next workspace next

# move workspace to screens
bindsym $mod+Shift+a move workspace to output left
bindsym $mod+Shift+p move workspace to output right


################################################
#============ WINDOW PARAMETERS ===============#
################################################

#Note: les exceptions doivent être en premier

#Workspace placement

# $wksp1
assign [class="Trilium Notes"] $wksp1
# $wksp2
assign [class="Thunar"] $wksp2
assign [class="Thunderbird"] $wksp2
# $wksp3
# $wksp4
# $wksp5
assign [instance="Navigator"] $wksp5
assign [class="Brave-browser"] $wksp5
# $wksp6
assign [class="discord"] $wksp6
# $wksp7
# $wksp8
assign [class="Emacs"] $wksp8
# $wksp9
# $wksp10

for_window [class="[.]*"]               floating enable
for_window [class="Thunar"]             floating disable
for_window [class="URxvt"]              floating disable
for_window [class="Code"]               floating disable
for_window [class="nvim-qt"]            floating disable
for_window [class="Evince"]             floating disable
for_window [instance="Navigator"]       floating disable
for_window [class="Thunderbird"]        floating disable
for_window [class="Trilium Notes"]      floating disable
for_window [class="discord"]            floating disable
for_window [class="Xmessage"]           floating enable, move absolute position center, focus, sticky enable
for_window [class="VirtualBox Machine"] floating disable
for_window [class="libreoffice"]        floating disable
for_window [class="Emacs"]              floating disable

for_window [window_role="pop-up"]       floating enable
for_window [window_role="bubble"]       floating enable 
for_window [window_role="task_dialog"]  floating enable
for_window [window_role="Preferences"]  floating enable
for_window [window_type="dialog"]       floating enable 
for_window [window_type="menu"]         floating enable

###############################################
#=============================================#
#=============================================#
#=============================================#
###############################################

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        set $size-px        2
        set $size-ppt       2
        set $big-size-px    6
        set $big-size-ppt   6
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width $size-px px or $size-ppt ppt
        bindsym j resize grow height $size-px px or $size-ppt ppt
        bindsym k resize shrink height $size-px px or $size-ppt ppt
        bindsym l resize grow width $size-px px or $size-ppt ppt
        bindsym Shift+h resize shrink width $big-size-px px or $big-size-ppt ppt
        bindsym Shift+j resize grow height $big-size-px px or $big-size-ppt ppt
        bindsym Shift+k resize shrink height $big-size-px px or $big-size-ppt ppt
        bindsym Shift+l resize grow width $big-size-px px or $big-size-ppt ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width $size-px px or $size-ppt ppt
        bindsym Down resize grow height $size-px px or $size-ppt ppt
        bindsym Up resize shrink height $size-px px or $size-ppt ppt
        bindsym Right resize grow width $size-px px or $size-ppt ppt
        bindsym Shift+Left resize shrink width $big-size-px px or $big-size-ppt ppt
        bindsym Shift+Down resize grow height $big-size-px px or $big-size-ppt ppt
        bindsym Shift+Up resize shrink height $big-size-px px or $big-size-ppt ppt
        bindsym Shift+Right resize grow width $big-size-px px or $big-size-ppt ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

# Resize mode
bindsym $mod+r mode "resize"

# | (h) hibernate 
set $mode_system [SYSTEM]: (l) logout | (s) suspend | (r) reboot | (Shift+S) shutdown
mode "$mode_system" {
    bindsym l exec --no-startup-id ~/.config/i3/scripts/i3exit logout, mode "default"
    bindsym s exec --no-startup-id ~/.config/i3/scripts/i3exit suspend, mode "default"
    #bindsym h exec --no-startup-id ~/.config/i3/scripts/i3exit hibernate, mode "default"
    bindsym r exec --no-startup-id ~/.config/i3/scripts/i3exit reboot, mode "default"
    bindsym Shift+s exec --no-startup-id ~/.config/i3/scripts/i3exit shutdown, mode "default"

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+Shift+Escape mode "$mode_system"

# Multimedia buttons for laptop
#https://faq.i3wm.org/question/3747/enabling-multimedia-keys.1.html
# Pulse Audio controls

#bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume 0 +5% #increase sound volume
#bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume 0 -5% #decrease sound volume
#bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute 0 toggle # mute sound

exec_always amixer -q -D pulse sset Master 50% && pkill -RTMIN+1 i3blocks
bindsym XF86AudioRaiseVolume exec amixer -q -D pulse sset Master 5%+ && pkill -RTMIN+1 i3blocks 
bindsym XF86AudioLowerVolume exec amixer -q -D pulse sset Master 5%- && pkill -RTMIN+1 i3blocks
bindsym XF86AudioMute exec amixer -q -D pulse sset Master toggle && pkill -RTMIN+1 i3blocks

# Sreen brightness controls
bindsym XF86MonBrightnessUp exec light -A 2 # increase screen brightness
bindsym XF86MonBrightnessDown exec light -U 2 # decrease screen brightness
bindsym Shift+XF86MonBrightnessUp exec light -A 10 # increase screen brightness
bindsym Shift+XF86MonBrightnessDown exec light -U 10 # decrease screen brightness

# Touchpad controls
exec_always ~/.config/i3/scripts/toggle_tp.sh once
exec_always syndaemon -i 0.60 -m 0.10 -d -k
bindsym $mod+Insert exec ~/.config/i3/scripts/toggle_tp.sh # toggle touchpad

# Media player controls
bindsym XF86AudioPlay exec playerctl play
bindsym XF86AudioPause exec playerctl pause
bindsym XF86AudioNext exec playerctl next
bindsym XF86AudioPrev exec playerctl previous

# Print controls
exec_always "touch /tmp/.screencaplock"
bindsym --release Print exec "$HOME/.config/i3/scripts/screencap.sh screen"
bindsym --release Shift+Print exec "$HOME/.config/i3/scripts/screencap.sh select"
bindsym --release Ctrl+Shift+Print exec "$HOME/.config/i3/scripts/screencap.sh clipboard"
bindsym --release Mod4+Print exec "source $HOME/.config/i3/scripts/screencap.sh && screencap_source_lock"

exec_always ~/.config/i3/scripts/picom_start.sh
exec_always --no-startup-id ~/.config/feh/themes/.fehrc
exec_always --no-startup-id urxvtd -q -o -f


set $bd-color			    #AE77C4
set $inactive-bd-color		#88B2D3
set $urgent-bd-color        #E06D79

set $bg-color			    #20334D
set $bg-color-wksp			#40536D
# set $inactive-bg-color		#122135
set $inactive-bg-color		#2C3544
set $urgent-bg-color		#A44040

set $text-color			    #f3f4f5
set $inactive-text-color	#878E9D

set $ind-color              #E06D79
set $inactive-ind-color     #54657E

bindsym $mod+p exec --no-startup-id rofi -run-list-command "rofi_aliases" -run-command "bash -i -c '{cmd}'" -show run -display-run ":: "

bindsym $mod+a exec tognav

#window colors			    border			    background			    text			        indicator               child_border
#
client.focused			    $bg-color		    $bg-color		        $text-color		        $ind-color              $bd-color
client.unfocused		    $inactive-bg-color	$inactive-bg-color		$inactive-text-color	$inactive-ind-color     $inactive-bd-color
client.focused_inactive		$inactive-bg-color	$inactive-bg-color		$inactive-text-color	$inactive-ind-color     $inactive-bd-color
client.urgent			    $urgent-bg-color	$urgent-bg-color		$text-color		        $urgent-ind-color       $urgent-bd-color

hide_edge_borders both
new_window pixel 1

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)

bar {
    status_command i3blocks -c ~/.config/i3/i3blocks/config
    tray_output primary
    font pango:tewi 8
    position bottom
    height 26
    i3bar_command i3bar -t
    colors {
    	   background $inactive-bg-color
	       #$bg-color bug (vert)
	       separator #757575
	       #status			    border			    background		    text
	       focused_workspace	$bg-color-wksp      $bg-color-wksp		$text-color
	       inactive_workspace	$inactive-bg-color	$inactive-bg-color	$inactive-text-color
	       urgent_workspace		$urgent-bg-color	$urgent-bg-color	$text-color
    }
}

# Customize lock screen
bindsym $mod+shift+x exec --no-startup-id i3lock -u -i $HOME/Wallpaper.png

# Customisation des gaps
gaps inner 20
gaps outer 20
#smart_gaps inverse_outer
smart_gaps on
#smart_borders no_gaps

# terminals
workspace 2 gaps outer 20
workspace 2 gaps inner 10

#Emacs
workspace 4 gaps outer 10
workspace 4 gaps inner 4

#TODOs
workspace 6 gaps outer 70
